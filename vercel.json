{
  "version": 2,
  "builds": [
    {
      "src": "src/app.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "src/app.ts" },
    { "src": "/(.*)", "dest": "build/app.js" }
  ],
  "env": {
    "NODE_OPTIONS": "--experimental-require-module"
  }
}
